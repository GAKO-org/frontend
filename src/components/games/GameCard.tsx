import { Game } from "@/types/game.dto";
import styled from "styled-components";

const GameCard = ({ game }: { game: Game }) => {
  return <Wrapper>{/* {game.} */}</Wrapper>;
};

const Wrapper = styled.div``;

export default GameCard;
